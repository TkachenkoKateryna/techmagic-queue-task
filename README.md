# techmagic-queue-task
- Clone git repo to your local device
- Open ```index.html``` in the browser
- If you are using ```VScode``` editor, open ```index.html``` in the editor and use ```LiveServer``` extension to run it
